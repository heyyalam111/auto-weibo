# 微博热搜产品创意分析器 - 使用说明

## 快速开始

### 方法1: 直接运行Python脚本

```bash
python weibo_analyzer.py
```

这将:
1. 自动获取当前微博热搜榜单(最多20条)
2. 分析每个热搜话题
3. 生成HTML报告
4. 自动在浏览器中打开报告

### 方法2: 在Claude Code中使用技能

在Claude Code中输入:
```
分析微博热搜
```

Claude会自动调用这个技能完成分析。

## 文件说明

- `weibo_analyzer.py` - 主执行脚本
- `skills/weibo-product-analyzer/SKILL.md` - Claude技能定义
- `skills/weibo-product-analyzer/templates/report_template.html` - HTML报告模板
- `reports/` - 生成的报告存放目录

## API配置

当前使用天行数据API:
- API地址: `https://apis.tianapi.com/weibohot/index`
- API Key: `76f000a3377212e17c8f5d716761f2f4`

如需更换API Key,请编辑 `weibo_analyzer.py` 中的 `API_KEY` 变量。

## 配置参数

在 `weibo_analyzer.py` 中可以调整:

```python
MAX_TOPICS = 20  # 最多分析的话题数量
MIN_TOPICS = 5   # 最少需要的话题数量
REPORT_DIR = "reports"  # 报告保存目录
```

## 输出示例

运行后会生成类似以下文件名的报告:
```
reports/微博热搜分析_2026-02-10_12-30.html
```

报告包含:
- 📊 分析概览(总数、优秀/良好项目数、平均分)
- 🏆 高分热点排行
- 📱 详细分析列表(评分、事件脉络、产品创意)

## 常见问题

### Q: API调用失败怎么办?
A: 脚本会自动重试3次,每次等待时间递增(5秒、10秒、15秒)。如果仍然失败,请检查:
- 网络连接是否正常
- API Key是否有效
- 是否达到API调用限制

### Q: 如何自定义评分标准?
A: 编辑 `weibo_analyzer.py` 中的 `analyze_topic()` 函数,调整评分逻辑。

### Q: 可以分析更多话题吗?
A: 修改 `MAX_TOPICS` 变量,但注意API可能有返回数量限制。

### Q: 报告可以自定义样式吗?
A: 编辑 `skills/weibo-product-analyzer/templates/report_template.html` 中的CSS样式。

## 依赖要求

```bash
pip install requests
```

Python 3.6+ 即可运行。

## 许可证

MIT License
